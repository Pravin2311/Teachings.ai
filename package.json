{
  "name": "teachings.ai",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build needed' && mkdir -p public && cp -r ./* public/",
    "start": "echo 'Static site'"
  },
  "dependencies": {
    "web-push": "^3.6.4"
  }
}